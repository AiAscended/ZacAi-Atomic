/**
 * Recurrent-neural-network - Layer Implementations
 */

import type { ModelPayload } from "../../shared/modelTypes"

export class RNNLayer {
  forward(input: ModelPayload): ModelPayload {
    return input
  }
}

export default RNNLayer
