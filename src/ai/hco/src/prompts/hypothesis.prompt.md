Generate three divergent hypotheses from atomic facts.

Constraints:
1. Temperature=1.0 (wild creativity allowed, but stay coherent).
2. Output JSON array of objects {"text","prior","type"}, where type ∈ {"wild","intuitive"}.
3. Priors are floats between 0 and 1.

Atomic facts: {facts}
