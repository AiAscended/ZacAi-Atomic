Extract atomic sentences ONLY. Convert natural language into Fa/Gab predicate logic.

Rules:
1. Sensorimotor precision, temp=0.2.
2. Output JSON array of strings (e.g., ["wise(socrates)", "teaches(socrates,plato)"]).
3. No commentary, explanations, or metadata.

Input: {input}
