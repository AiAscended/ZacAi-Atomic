/**
 * File: src/ai/training/adversarialTrainer.ts
 * Purpose: Minimal adversarial example generator stub for robustness testing.
 */

export const generateAdversarial = (text: string) => {
  // Very small stub: return a perturbed text
  return text.split('').reverse().join('');
};
