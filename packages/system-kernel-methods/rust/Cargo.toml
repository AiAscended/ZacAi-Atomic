[package]
name = "zk_kernels"
version = "0.1.0"
authors = ["ZacAi"]
edition = "2021"

[lib]
name = "zk_kernels"
crate-type = ["cdylib","rlib"]

[dependencies]
wasm-bindgen = { version = "0.2", optional = true }

[features]
wasm = ["wasm-bindgen"]

[dev-dependencies]
# add testing deps as needed

[profile.release]
opt-level = 3
lto = true
debug = false
strip = "debuginfo"
