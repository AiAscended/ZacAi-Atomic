{
  "name": "zacai-system-core",
  "version": "1.0.0",
  "description": "ZacAi System Core v0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "node apps/web/server.js",
    "dashboard": "node apps/web/server.js",
    "build": "npm run build:packages",
    "build:packages": "echo '📦 Building all packages...' && (cd packages/system-kernel && npm run build 2>/dev/null || echo '⚠️  Kernel build skipped') && (cd packages/system-kernel-methods && npm run build 2>/dev/null || echo '⚠️  Methods build skipped') && (cd packages/system-core && npm run build 2>/dev/null || echo '⚠️  Core build skipped') && echo '✅ Build complete'",
    "test": "echo '🧪 Testing - setup required'",
    "lint": "echo '🔍 Linting - setup required'",
    "clean": "rm -rf node_modules */node_modules **/dist **/node_modules",
    "install:all": "pnpm install --recursive",
    "start": "node apps/web/server.js",
    "preview": "node tools/preview-server.js"
  },
  "packageManager": "pnpm@8.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
